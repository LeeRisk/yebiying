<%= form_for(@epiphany) do |f| %>
  <% if @epiphany.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@epiphany.errors.count, "error") %> prohibited this epiphany from being saved:</h2>

      <ul>
      <% @epiphany.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
